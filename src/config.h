#pragma once

/*
 * Runtime config via LittleFS /config.txt is disabled.
 * See app_config.h.
 *
struct AppConfig {
    bool usb;
    bool wifi;
    char ssid[33];
    char password[65];
    char hw_version[24];
};

AppConfig config_init();
*/
